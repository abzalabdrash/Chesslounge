# Featherless API key for live AI commentary.
# Sign up at https://featherless.ai and create a key.
# Without this, commentary falls back to prebaked persona lines (offline mode).
# Copy this file to .env.local and fill in your key (.env.local is gitignored).
VITE_FEATHERLESS_API_KEY=
